"""
ex4.py

Код для решение пятого практического задания: 'сложные пароли'

Основные возможности:
    1. Генерировать пароль
    2. Выставлять настройки пароля:
        • Наличие нижнего регистра
        • Верхнего регистра
        • Спец символов
        • Цифр
        • Длинна пароля
    3. Приятный, понятный и красивый интерфейс терминала
"""
def arabic_to_roman(num):
    """
    Конвертирует арабское число в римское.
    
    Args:
        num (int): Арабское число для конвертации
    
    Returns:
        str: Римское число
    """
    # Список кортежей (значение, символ) в порядке убывания значений
    # Включает как основные символы, так и комбинации для вычитания (IV, IX, XL и т.д.)
    val = [
        (1000, 'M'), (900, 'CM'), (500, 'D'), (400, 'CD'),
        (100, 'C'), (90, 'XC'), (50, 'L'), (40, 'XL'),
        (10, 'X'), (9, 'IX'), (5, 'V'), (4, 'IV'),
        (1, 'I')
    ]
    result = []  # Список для накопления римских символов
    
    # Проходим по всем значениям от наибольшего к наименьшему
    for value, symbol in val:
        # Пока текущее число больше или равно значению символа
        while num >= value:
            result.append(symbol)  # Добавляем символ в результат
            num -= value           # Вычитаем значение из числа
    
    return ''.join(result)  # Объединяем символы в строку


def roman_to_arabic(roman):
    """
    Конвертирует римское число в арабское.
    
    Args:
        roman (str): Римское число для конвертации
    
    Returns:
        int: Арабское число
    """
    # Словарь для преобразования отдельных римских символов в числовые значения
    symbols = {
        'I': 1, 'V': 5, 'X': 10, 'L': 50,
        'C': 100, 'D': 500, 'M': 1000
    }
    result = 0       # Переменная для накопления результата
    prev_value = 0   # Значение предыдущего символа (инициализируем 0)
    
    # Обрабатываем строку справа налево (с младших разрядов к старшим)
    for char in reversed(roman):
        current_value = symbols[char]  # Получаем числовое значение текущего символа
        
        # Логика вычитания: если текущий символ меньше предыдущего,
        # значит это случай вычитания (например, IV = 5 - 1 = 4)
        if current_value < prev_value:
            result -= current_value  # Вычитаем значение
        else:
            result += current_value  # Иначе прибавляем значение
        
        prev_value = current_value  # Обновляем предыдущее значение для следующей итерации
    
    return result